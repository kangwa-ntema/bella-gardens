
/* 
const primaryNavLinks = document.querySelectorAll(".primaryNavLink");

console.log(primaryNavLinks);

const toggleActiveClass = (e) => {
  primaryNavLinks.forEach((primaryNavLink) =>
    primaryNavLink.classList.remove("active")
  );
  e.target.classList.toggle("active");
};

primaryNavLinks.forEach((primaryNavLink) =>
  primaryNavLink.addEventListener("click", toggleActiveClass)
);
 */